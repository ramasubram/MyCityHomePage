//  Javascript for search

const form = document.querySelector('form');

form.addEventListener('submit', (e) => {
    e.preventDefault();
    let inputSearchValue = (document.querySelector('.inputSearchValue').value);
});